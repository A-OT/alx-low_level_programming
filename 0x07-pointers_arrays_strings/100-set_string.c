#include "main.h"

/**
 * set_string - Function that sets value of a pinter to a char.
 *
 * @s: input.
 * @to: the char to point to.
 *
 * Return: 0 (success).
 */

void set_string(char **s, char *to)
{
	*s = to;
}
